# 新目录运行记录（2026-09-24）

**结论：新目录安装、工作台启动与固定模板的 Engine 构建/预览已实测通过；从参考图到最终场景的模型驱动端到端验收未通过。** 不应把“服务可访问”“测试通过”或“固定模板可运行”写成完整场景生成成功。

## 环境与候选

- 从公开仓库克隆到全新目录 `~/extensions/test/scene-foundry-clean-20260924/scene-foundry`，未复制旧目录的 `.env`、数据和依赖。验证的应用源码候选为 `3a120a75739b3b9b5fb44b1f69c53b86d6fdc85d`；此后的文档和测试修订须另行检查。
- `python3 prototype/bin/bootstrap.py` 在该目录克隆并构建私有依赖：Engine `f2eb6c0893f1df2310514b95e76dad24c6d2fc0d`，Scene Generator `c3943a5026fc5f32f1b4993d542e1f372656049c`。
- 工作台独立运行在 `127.0.0.1:19977`，原有 `127.0.0.1:19774` 未被替换；固定模板预览使用 `localhost:19978`。模型设置为 Codex CLI `gpt-6-luna` / `low`，这个新目录的累计调用上限为 30。
- 发起长任务前，已用一次短请求核对 CLI 实际返回 `model: gpt-6-luna`、`provider: openai`、`reasoning effort: low`；工作台 `/api/state` 和模型列表也显示相同配置。首个模型调用回执再次确认了该路由。

## 已通过的检查

| 检查 | 实测结果 | 范围 |
|---|---|---|
| 新克隆依赖准备 | 固定提交克隆、安装并构建成功 | 私有仓库访问、依赖可重建 |
| 工作台源码测试 | 拉取手册和测试修订后，`(cd workbench && bun test ./src)` 为 324/324 通过 | 包含本机 `.env` 选用 Luna 的情况 |
| 固定模板测试 | `(cd prototype && bun test bin/pipeline.test.ts)` 为 3/3 通过 | 从模板目录运行，避开历史任务快照 |
| 工作台启动 | `19977` 首页、状态和模型列表可访问 | 服务与配置，不证明生成质量 |
| 正常网页入口 | 上传本仓库脚本生成的 PNG，填写描述，确认参考图并开始生成 | 浏览器正常提交流程，非直写任务 |
| 固定模板 `generate → build → verify` | 全部阶段通过；场景 1,232 三角形、78 网格、8 材质，资产目录 95 条 | 程序化样例的 Engine 导出、构建与资产校验 |
| 固定模板预览 | `localhost:19978` 返回 200，浏览器可见三维温室场景 | 静态预览画面；未执行模板的完整交互验收 |

固定模板的报告保存在新目录被 Git 忽略的 `prototype/evidence/run-report.json`；其中 `browser.status` 仍为 `not-run`，`spec.status` 为 `pending-visual-review`。浏览器中看到了场景，但未生成该模板 `accept` 所需的完整交互截图与观察记录。

## 未通过的模型生成

从正常网页入口提交一个简单灯塔参考图任务，并通过页面“重新开始未完成任务”对明确的代码/提示约束缺陷作了有限复验。四个任务都保留在新目录的 `workbench/data/runs/`，状态均为 `failed`：

| 任务 | 停止阶段 | 可复核的失败原因 |
|---|---|---|
| `0c483b05-ac28-4fb8-bef4-9804d1ffc855` | `generate` | 模型给出从 0 开始的参考图索引，地标图片证据未通过校验 |
| `d96e74e3-7d5a-44bb-909d-d597a6748844` | `graybox` | 灯塔几何超出共享布局边界 |
| `8bef84c5-feae-4326-a1d6-b259a9b1102f` | `graybox` | 灰模使用了该阶段禁止的散布几何，部件约束失败 |
| `9e9f3057-d1bd-4d89-b29f-140420539a92` | `graybox` | `sky_template` 的 Z 范围为 `[-12, 6]`，超出允许的 `[-5, 20]`（容差 1.25）；格式纠正后仍未收敛 |

前三次暴露的索引、边界诊断、灰模 schema/提示冲突已在候选源码中修复并用定向测试检查。第四次仍停在同类几何边界校验，尚未到模型资产生成、Engine 构建、运行截图、视觉评判或最终门禁。此轮累计使用 19/30 次模型调用后停止重复付费尝试；没有切换模型、伪造产物或跳过正常网页流程。要证明完整运行，需要针对仍未收敛的几何边界形成新修复假设，再以新的固定候选作有界复验。
